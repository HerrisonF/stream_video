name: teste_seventh
description: Teste para Seventh

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  mobx: ^1.2.1+4
  get_it: ^5.0.6
  flutter_mobx: ^1.1.0+2
  dio: ^3.0.10
  flutter_svg: ^0.22.0
  shared_preferences: ^2.0.4
  video_player:
  chewie:
  flutter_plugin_android_lifecycle: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.1
  mobx_codegen: ^1.1.2

flutter:

  uses-material-design: true

  assets:
    - images/
